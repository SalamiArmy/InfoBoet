import main
getvid = main.get_platform_command_code('telegram', 'getvid')
def run(bot, chat_id, user, keyConfig, message, totalResults=1):
    getvid.run(bot, chat_id, user, keyConfig, message, totalResults)
